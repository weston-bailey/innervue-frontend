import React from 'react';
import AppBar from '../content/components/AppBar'


const NavBar = (props) => {
    return (
        <div>
        <AppBar {...props} />
        </div>
    )
}

export default NavBar;